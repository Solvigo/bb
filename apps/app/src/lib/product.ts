/**
 * The product's name, wherever the app says it about itself.
 *
 * It lives here because a title set at runtime wins over the one in
 * index.html, and the app names itself at four such points: an explicit title
 * per route, two fallbacks for routes without one, and a system message that
 * names its own sender. Renaming any subset of those looks complete and is
 * not.
 *
 * NOT for the CLI binary, package names, storage keys or plugin identifiers.
 * Those are addresses, not branding, and they keep the name they answer to.
 */
export const PRODUCT_NAME = "Solvigo Airways";
